spring:
  application:
    name: zipkin-server

server:
  port: 9411

# Eureka \u5BA2\u6237\u7AEF\u914D\u7F6E
eureka:
  client:
    serverUrl:
      defaultZone: http://localhost:8761/eureka #eureka

management:
  metrics:
    web:
      server:
        auto-time-requests: false #\u89E3\u51B3\u8BBF\u95EE500 Exception handling request to /
